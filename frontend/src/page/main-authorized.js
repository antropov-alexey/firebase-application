import Vue from 'vue';
import MainAuthorized from '../components/MainAuthorized';

new Vue({
    el        : '#main-authorized',
    components: {
        'main-authorized': MainAuthorized,
    },
});
